# Mirror / 镜 —— 技术方案（开发主蓝图）

> 本文件是给开发者（Sonnet）的**逐项开发蓝图**，配合 `CLAUDE.md`（铁律）、`项目上下文_必读.md`（宪法）、`有机本体设计.md`（内核规格）、`开发进度.md`（状态机）使用。
> **怎么用本方案**：先通读第 1~9 节理解架构与契约，开发时按第 10 节「里程碑」逐任务推进；每个任务做完按 DoD 验收，并更新 `开发进度.md`。
> **最后更新**：2026-05-30

---

## 1. 总体架构（三层栈 + 数据流）

```
┌─────────────────────────────────────────────────────────────┐
│ Layer 2 · 认知身体 Body（前端 / 浏览器）                       │
│  认知星云(r3f+shader+bloom) · 视觉映射 · 对质重组 · 时间机器     │
│  · 证据链溯源高亮 · 否决权UI · 分享卡 · 加载动画                 │
├─────────────────────────────────────────────────────────────┤
│ Layer 1 · 认知引擎 Engine                                      │
│  ┌ 前端侧：embedding(transformers.js) · UMAP降维 · 衰减计算     │
│  └ serverless侧：五Agent编排 · 校准 · 信念修正                  │
│     经 LLMProvider 抽象层 → 默认 DeepSeek（可配置）             │
├─────────────────────────────────────────────────────────────┤
│ Layer 0 · 数据入口 Ingestion（前端 / 浏览器）                  │
│  预置seed数据 · 现场补一句 · 导入(ChatGPT/Claude导出)           │
│  持久化：IndexedDB(Dexie) —— 数据不出本机                      │
├─────────────────────────────────────────────────────────────┤
│ ╳ 横切：本地优先 · 用户自带key · LLM Provider 可配置            │
└─────────────────────────────────────────────────────────────┘
```

**数据流（一次"现场补一句"）**：
用户输入 → 存入 IndexedDB(原始对话) → 调 `/api/infer`(serverless 跑五 Agent) → 返回图增量(节点+事件+主动提问) → 前端算 embedding+UMAP 坐标 → 更新 Zustand store → 星云生长/重组动画。

**关键解耦**（满足开源/集成）：
- Layer 1 serverless 部分(五 Agent)只依赖 `LLMProvider` 接口，不认识具体厂商。
- 引擎产出的"图"(CognitiveGraph)是标准数据结构，Layer 2 只消费它——身体可被替换、引擎可被单独集成。

---

## 2. 技术栈（已锁定）

| 类别 | 选型 | 备注 |
|---|---|---|
| 框架 | **Next.js (App Router) + TypeScript** | 前端 + serverless 一体；部署 Vercel |
| 样式 | **Tailwind CSS** | |
| 3D 可视化 | **three + @react-three/fiber + @react-three/drei + @react-three/postprocessing** | bloom 辉光 + 自定义 GLSL |
| 状态管理 | **Zustand** | 全局 store |
| 本地存储 | **Dexie.js (IndexedDB)** | 数据不出本机 |
| LLM | **可配置 Provider，默认 DeepSeek**：`deepseek-chat`(抽取/推断) + `deepseek-reasoner`(对质审议) | 经 serverless 代理，OpenAI 兼容 |
| Embedding | **可配置(EmbeddingProvider)**，默认 **@xenova/transformers** 浏览器本地 `Xenova/all-MiniLM-L6-v2`；可选云端 | 默认零 key、数据不出本机 |
| 降维 | **umap-js** | 浏览器端，语义坐标 |
| Agent 编排 | **自研轻量编排**（顺序 pipeline + 结构化输出 + Zod 校验 + 重试） | 不引 LangChain |
| 数据校验 | **Zod** | 校验所有 LLM 输出 |
| UI 动效(2D) | **framer-motion**（侧栏/面板过渡，可选） | 3D 动画走 r3f useFrame |

依赖安装清单（M0 用）：
```
next react react-dom typescript
tailwindcss postcss autoprefixer
three @react-three/fiber @react-three/drei @react-three/postprocessing
zustand dexie zod openai umap-js @xenova/transformers
framer-motion
```
> 说明：`openai` SDK 用于 OpenAI 兼容端点（DeepSeek 等）；Claude adapter 用 `@anthropic-ai/sdk`（仅在切到 Claude 时需要）。

测试依赖（devDependencies）：
```
vitest @vitest/coverage-v8 @testing-library/react @testing-library/jest-dom jsdom @playwright/test fake-indexeddb
```
> 测试与验收标准详见《测试方案.md》。**硬约束**：Agent/pipeline 必须支持注入 provider 以便 mock。

---

## 3. 项目目录结构

```
mirror/
├── CLAUDE.md  项目上下文_必读.md  有机本体设计.md  开发进度.md  技术方案.md
├── .env.example                    # 环境变量模板（含 LLM 可配置项）
├── package.json  tsconfig.json  tailwind.config.ts  next.config.js
├── public/
│   └── models/                     # （可选）自托管 embedding 模型权重
├── src/
│   ├── app/
│   │   ├── page.tsx                # 主页面（星云）
│   │   ├── layout.tsx
│   │   └── api/
│   │       ├── infer/route.ts      # 跑五 Agent 推断
│   │       └── confront/route.ts   # 对质（流式思考态）
│   ├── core/                       # ⭐ Layer 1 引擎（可被单独抽取/集成）
│   │   ├── types.ts                # 核心数据模型
│   │   ├── schemas.ts              # Zod schemas
│   │   ├── llm/
│   │   │   ├── provider.ts         # LLMProvider 抽象接口
│   │   │   ├── openai-compatible.ts# DeepSeek/Qwen/豆包/OpenAI adapter
│   │   │   ├── anthropic.ts        # Claude adapter
│   │   │   └── factory.ts          # 按 env 选 provider
│   │   ├── agents/
│   │   │   ├── observer.ts  inferer.ts  skeptic.ts
│   │   │   ├── reconciler.ts  reflector.ts
│   │   │   └── pipeline.ts         # 编排
│   │   ├── calibration.ts          # 置信度校准
│   │   ├── embedding/              # ⭐ 可配置 embedding（与 llm 对称）
│   │   │   ├── provider.ts         # EmbeddingProvider 抽象接口
│   │   │   ├── local-minilm.ts     # 浏览器本地 MiniLM（默认，transformers.js worker）
│   │   │   ├── cloud-openai.ts     # 云端 embedding（可选：DeepSeek/OpenAI/Voyage，经 /api/embed）
│   │   │   └── factory.ts          # 按 env 选 provider
│   │   ├── layout.ts               # UMAP + 星团引力布局
│   │   └── decay.ts                # 衰减/显著性
│   ├── data/
│   │   ├── db.ts                   # Dexie schema
│   │   ├── seed.ts                 # 预置"三个月"星云
│   │   └── import.ts               # ChatGPT/Claude 导出解析
│   ├── store/
│   │   └── useMirrorStore.ts       # Zustand
│   ├── viz/                        # Layer 2 身体
│   │   ├── Nebula.tsx              # 星云粒子系统
│   │   ├── nebula.glsl.ts          # shader 源码
│   │   ├── visualMapping.ts        # 节点字段 → 视觉属性
│   │   ├── Confrontation.tsx       # 对质状态机 + 动画
│   │   ├── TimeMachine.tsx         # 演变层回放
│   │   ├── NodeInspector.tsx       # 详情/证据链/否决权
│   │   ├── ShareCard.tsx           # 分享卡
│   │   └── LoadingNebula.tsx       # 初始化动画
│   └── lib/                        # 通用工具
└── tests/                          # schema/agent 单测
```

---

## 4. 核心数据模型（`src/core/types.ts` + `schemas.ts`）

> 这是整个系统的 SSOT。所有层共享。Zod schema 用于校验 LLM 输出与持久化数据。

```ts
// ── 认知系统（五星团，对应有机本体设计.md 第三节） ──
export type CognitiveSystemId =
  | 'narrative'  // 叙事核 · 默认模式网络DMN
  | 'affect'     // 情动潮汐 · 杏仁核/边缘系统
  | 'valence'    // 趋避场 · 眶额皮层OFC
  | 'social'     // 他者之镜 · 颞顶联合区TPJ
  | 'rhythm';    // 节律之根 · 基底神经节

export const SYSTEM_META: Record<CognitiveSystemId, {
  poeticName: string; brainRegion: string; color: string; isPrimary: boolean;
  anchor: [number, number, number]; // 星团引力中心（布局用）
}> = {
  narrative: { poeticName:'叙事核', brainRegion:'默认模式网络', color:'#F2D399', isPrimary:true,  anchor:[0,0.6,0] },
  affect:    { poeticName:'情动潮汐', brainRegion:'杏仁核·边缘系统', color:'#FF6B5E', isPrimary:true, anchor:[0.9,0.1,0.2] },
  valence:   { poeticName:'趋避场', brainRegion:'眶额皮层', color:'#5EE0C0', isPrimary:false, anchor:[-0.9,0.0,0.3] },
  social:    { poeticName:'他者之镜', brainRegion:'颞顶联合区', color:'#6FA8FF', isPrimary:false, anchor:[0.3,-0.2,-0.9] },
  rhythm:    { poeticName:'节律之根', brainRegion:'基底神经节', color:'#B08D57', isPrimary:false, anchor:[0,-0.9,0] },
};

// ── 三层记忆（对应神经真身） ──
export type MemoryLayer = 'fact' | 'inference' | 'evolution';

// ── 证据链 ──
export interface Evidence {
  id: string;
  sourceMessageId: string; // 指回原始对话消息
  quote: string;           // 原文片段（供溯源高亮）
  timestamp: string;       // ISO
}

// ── 认知节点（事实 or 推断） ──
export interface CognitiveNode {
  id: string;
  layer: 'fact' | 'inference';
  system: CognitiveSystemId;
  statement: string;            // "你倾向于回避需要自己做主的决定"
  confidence: number;           // 0~1，校准后
  rawConfidence: number;        // 0~1，LLM 原始自评（校准前，保留以画校准曲线）
  evidence: Evidence[];         // 推断必须 ≥1 条；事实可为用户直述
  embedding?: number[];         // 语义向量（前端填充）
  position?: [number, number, number]; // UMAP+引力 坐标（前端填充）
  salience: number;             // 0~1 情绪显著性（影响亮度/抗衰减）
  lastReinforced: string;       // ISO，衰减计算用
  createdAt: string;
  status: 'active' | 'decaying' | 'pruned';
  userVerdict: 'confirmed' | 'denied' | 'dont_guess' | null; // 否决权
  isUncanny: boolean;           // 恐怖谷标记（怀疑者判定）
  skepticFlag: string | null;   // 怀疑者的质疑文本（如"仅基于1次对话，可能过拟合"）
  contradicts: string[];        // 与哪些 node id 矛盾（张力线）
}

// ── 演变事件（演变层 = 事件溯源） ──
export interface EvolutionEvent {
  id: string;
  nodeId: string;
  type: 'created'|'reinforced'|'revised'|'denied'|'reconsolidated'|'decayed';
  fromStatement?: string;
  toStatement?: string;
  fromConfidence?: number;
  toConfidence?: number;
  reason: string;               // "用户指出反例：上周主动定了旅行计划"
  triggeredBy: 'observation'|'reconciliation'|'user_confrontation';
  timestamp: string;
}

// ── 主动提问（信息增益驱动） ──
export interface ActiveQuestion {
  id: string;
  question: string;             // AI 主动问的问题
  targetSystem: CognitiveSystemId;
  reason: string;               // "对你在'关系'维度的不确定性最高"
  expectedInfoGain: number;     // 0~1（估计）
}

// ── 整张图（引擎产出的标准结构） ──
export interface CognitiveGraph {
  nodes: CognitiveNode[];
  events: EvolutionEvent[];
  questions: ActiveQuestion[];
  rawMessages: RawMessage[];    // 原始对话（证据溯源用）
}

export interface RawMessage {
  id: string; role: 'user'|'assistant'; content: string; timestamp: string;
}
```

> `schemas.ts`：为 `CognitiveNode`、`EvolutionEvent`、`ActiveQuestion`、以及每个 Agent 的输出各写一个 Zod schema。Agent 输出**必须**过 Zod 校验，失败则按第 5.3 重试。

---

## 5. Layer 1 · 认知引擎

### 5.1 LLM Provider 抽象层（可配置核心）

```ts
// src/core/llm/provider.ts
export interface LLMMessage { role:'system'|'user'|'assistant'; content:string }

export interface ChatRequest {
  system: string; messages: LLMMessage[];
  temperature?: number; model?: 'fast'|'reasoning'; // 语义档位，由 adapter 映射到具体模型
  stream?: boolean;
}
export interface ChatResult { text: string; reasoning?: string } // reasoning = R1 思考过程

export interface StructuredRequest<T> {
  system: string; messages: LLMMessage[];
  schema: import('zod').ZodType<T>; schemaName: string; temperature?: number;
}

export interface LLMProvider {
  chat(req: ChatRequest): Promise<ChatResult>;
  chatStream?(req: ChatRequest): AsyncIterable<{ delta:string; reasoningDelta?:string }>;
  structured<T>(req: StructuredRequest<T>): Promise<T>; // 内部负责 JSON 解析 + Zod 校验 + 重试
}
```

- **`openai-compatible.ts`**：用 `openai` SDK，`baseURL`/`apiKey`/模型名全来自 env。覆盖 DeepSeek、Qwen(DashScope 兼容模式)、豆包、OpenAI。`structured()` 优先用 JSON 输出模式（DeepSeek 支持 `response_format`），再 `JSON.parse` + Zod；失败把校验错误回灌重试（≤2 次）。`reasoning` 取 DeepSeek `reasoning_content` 字段。
- **`anthropic.ts`**：用 `@anthropic-ai/sdk`，`structured()` 走 tool use（强制工具）拿结构化结果。
- **`factory.ts`**：读 `LLM_PROVIDER` 返回对应 adapter。`model:'fast'` → `LLM_MODEL_FAST`，`'reasoning'` → `LLM_MODEL_REASONING`。

`.env.example`：
```
LLM_PROVIDER=deepseek                 # deepseek | qwen | doubao | openai | anthropic
LLM_API_KEY=sk-xxxx                    # 服务端密钥（serverless 用，不暴露前端）
LLM_BASE_URL=https://api.deepseek.com  # OpenAI 兼容端点
LLM_MODEL_FAST=deepseek-chat           # 抽取/推断/怀疑/调和
LLM_MODEL_REASONING=deepseek-reasoner  # 对质审议（思考态可视化）
DEMO_DAILY_LIMIT=200                   # 演示用限额（防 key 被刷）

# ── Embedding（可配置，默认本地零 key）──
NEXT_PUBLIC_EMBEDDING_PROVIDER=local   # local（浏览器本地）| cloud（云端）
NEXT_PUBLIC_EMBEDDING_MODEL=Xenova/all-MiniLM-L6-v2  # 本地模型
# 仅 cloud 模式需要（走 /api/embed，key 不暴露前端）：
# EMBEDDING_API_KEY=
# EMBEDDING_BASE_URL=https://api.openai.com/v1
# EMBEDDING_CLOUD_MODEL=text-embedding-3-small
```

### 5.2 五 Agent 规格

> 全部经 `provider.structured()` 输出结构化 JSON。每个 Agent 一个文件，导出一个纯函数。
> 预测加工框架的落地：Inferer=生成预测，Skeptic=算预测误差，Reconciler=用误差更新先验，Reflector=维护全局生成模型。

| Agent | 输入 | 输出 | system prompt 要点 |
|---|---|---|---|
| **Observer** `observer.ts` | 新对话消息 + 近期上下文 | `{ facts: Fact[], signals: BehavioralSignal[] }`（signals 含语气/回避/犹豫等行为信号） | "你只抽取，不推断。区分①用户明说的事实 ②行为信号（回避/犹豫/强调/情绪词）。每条标出处 messageId。" |
| **Inferer** `inferer.ts` | signals + 现有图 | `CognitiveNode[]`（layer=inference，带 rawConfidence + evidence + system 归类 + salience） | "基于信号生成关于用户的**预测/假设**。每条：①归入五系统之一 ②给原始置信度（**单一证据≤0.6**）③附 evidence 引用原句 ④估情绪显著性。允许大胆，但必须可溯源。" |
| **Skeptic** `skeptic.ts` | Inferer 产出的节点 | 每节点 `{ skepticFlag, isUncanny, keep:boolean }` | "红队审查：是否过拟合一次对话？是否越界冒犯（恐怖谷）？标 isUncanny。给出质疑文本。守'恐怖谷边界但保留张力'——准得发毛的保留并标记，冒犯人格尊严的剔除。" |
| **Reconciler** `reconciler.ts` | 新节点 + 现有图（+ 用户否决/对质） | `{ revisedNodes, events: EvolutionEvent[], contradictions }` | "检测新旧矛盾（同系统语义冲突）。冲突时做信念修正：用预测误差更新先验，产出 revised/reconsolidated 事件，解释原因。" |
| **Reflector** `reflector.ts` | 全图 | `{ globalNotes, biasWarnings, decayCandidates: string[] }` | "元认知：整张画像是否自洽？有无偏见/刻板印象？哪些长期未强化应衰减？" |

`Fact`/`BehavioralSignal` 类型在 types.ts 补充定义。

### 5.3 编排 `pipeline.ts`

```
runInference(newMessages, currentGraph):
  signals  = Observer(newMessages, currentGraph)
  draft    = Inferer(signals, currentGraph)
  judged   = Skeptic(draft)            // 过滤 keep=false，附 skepticFlag/isUncanny
  recon    = Reconciler(judged, currentGraph)  // 矛盾→修正→events
  reflect  = Reflector(merge(currentGraph, recon))
  questions = deriveActiveQuestions(reflect)    // 信息增益：挑不确定性最高的系统提问
  → 应用校准(5.4) → 返回 CognitiveGraph 增量
```
- 每步 Zod 校验，失败重试 ≤2；仍失败则跳过该步并记 warning（不阻断整条）。
- serverless `/api/infer` 调用此函数。

### 5.4 可信三件套

1. **置信度校准 `calibration.ts`**
   - 先验规则（冷启动）：`证据条数=1 → cap 0.6`；`跨≥2个时间点的多证据 → cap 0.85`；`被用户 confirmed 历史相关项 → +bonus`。
   - 在线校准：维护置信度分桶 `[0-0.2,...,0.8-1]`，记录每桶被 confirmed/denied 的实际命中率；用分桶实际命中率反向映射 rawConfidence→confidence。数据存 IndexedDB。
   - 暴露 `getCalibrationCurve()` 供 UI 画"声称置信度 vs 实际命中率"曲线（开源加分）。
2. **证据链溯源**：节点 `evidence[].sourceMessageId` 指回 `rawMessages`。NodeInspector 点开 → 高亮原句。
3. **信念修正**：Reconciler 产出 `revised`/`reconsolidated` 事件；演变层据此回放。

### 5.5 Embedding（可配置）+ 布局（前端）

**EmbeddingProvider 抽象**（与 LLMProvider 对称）：
```ts
// src/core/embedding/provider.ts
export interface EmbeddingProvider {
  embed(texts: string[]): Promise<number[][]>;  // 批量文本 → 向量
  readonly dim: number;                          // 向量维度（MiniLM=384）
}
```
- **`local-minilm.ts`（默认）**：在 **Web Worker** 里用 `@xenova/transformers` 加载 `Xenova/all-MiniLM-L6-v2`，浏览器本地推理，零 key。首次加载模型由 `LoadingNebula` 动画掩盖。
- **`cloud-openai.ts`（可选）**：走 serverless `/api/embed`（key 藏服务端），OpenAI 兼容 embedding 端点，覆盖 OpenAI/DeepSeek/Voyage。
- **`factory.ts`**：读 `NEXT_PUBLIC_EMBEDDING_PROVIDER` 选择；业务层只依赖 `EmbeddingProvider` 接口。
- 向量结果一律缓存进 IndexedDB（按 statement hash），避免重复计算。
- ⚠️ 切换 provider 若 `dim` 变化，需重算全部 embedding（layout 依赖一致维度）。
- `layout.ts`：`umap-js` 对所有节点 embedding 做 3D 降维 → 得到语义坐标；再叠加该节点所属系统的 `anchor` 引力偏移（`pos = umapPos * w + anchor * (1-w)`，w≈0.6），使**同系统聚拢又保留语义结构**。
- `decay.ts`：`opacity = clamp(base * salience * decayFactor(now - lastReinforced))`；衰减节点 y 轴下沉；超阈值标 `pruned`。

---

## 6. Layer 0 · 数据入口

- **`db.ts`(Dexie)**：表 `nodes`、`events`、`messages`、`calibration`、`meta`。提供 CRUD + 订阅。
- **`seed.ts`**：手工编写一份"已认识你三个月"的丰满数据——约 40~60 节点覆盖五系统、若干 EvolutionEvent（含"上月以为X→本周改判Y"）、**1~2 条恐怖谷推断**（如"你最近在为一件没说出口的事焦虑"，带 isUncanny + skepticFlag + 否决入口）、以及一段**预设对质脚本**（无需 key 即可演示重组）。
- **现场补一句**：输入框 → 存 messages → 调 `/api/infer` → 应用增量 → 触发星云生长。
- **`import.ts`**：解析 ChatGPT(`conversations.json`)/Claude 导出 → 规整成 `RawMessage[]` → 分批跑 pipeline（带进度）。

---

## 7. Layer 2 · 认知身体

### 7.1 星云渲染 `Nebula.tsx` + `nebula.glsl.ts`
- 用 `THREE.Points` + 自定义 `ShaderMaterial`；每节点一个粒子。
- 顶点 attributes：`position`、`aColor`、`aSize`、`aOpacity`、`aPulse`、`aJitter`。
- `useFrame` 传 `uTime`；shader 内做呼吸脉动、抖动、辉光衰减。
- 后处理：`@react-three/postprocessing` 的 `Bloom`（深空辉光）+ 轻微 `DepthOfField`（景深体积感）。
- 背景深空、缓慢相机轨道。

### 7.2 视觉映射 `visualMapping.ts`（红线：每个视觉量=真实内部量）
```ts
function nodeToVisual(n: CognitiveNode) {
  return {
    position: n.position,
    color:    SYSTEM_META[n.system].color,        // 所属系统
    size:     lerp(0.5, 1.5, n.confidence),        // 置信度
    opacity:  n.confidence * decayFactor(n) ,      // 置信度×衰减
    pulse:    n.salience,                           // 情绪显著性
    jitter:   n.skepticFlag ? 0.4 : 0,             // 怀疑者质疑→抖动
    tension:  n.contradicts.length>0,              // 矛盾→张力线
    layerDepth: layerToDepth(n.layer),             // 核心/外围/底层
    uncannyFlicker: n.isUncanny,                   // 恐怖谷→幽暗闪烁
  };
}
```
矛盾对之间画 `tension` 张力线（`drei <Line>`，颜色随张力强度）。

### 7.3 对质 `Confrontation.tsx`（= 记忆重固化状态机）
状态机 phase：`idle → retrieving(点亮目标节点+证据) → plasticizing(目标液化:粒子扩散软化) → deliberating(侧栏流式显示 deepseek-reasoner 的 reasoning，即思考态) → restructuring(节点迁移/重连/暗灭，lerp 动画) → settled(写入 EvolutionEvent，演变层加一层沉积)`。
- 流式：`/api/confront` 用 `chatStream` 返回 `reasoningDelta`，前端实时渲染审议过程。
- 用户路径：①点节点 → 看证据链 → ②"这条判断错了，因为…" → 触发上述时序。

### 7.4 其他
- **`NodeInspector.tsx`**：节点详情；双层脑区名（默认诗意名，hover 出真实脑区）；证据链原句高亮；**否决权**按钮（确认/否认/"别瞎猜"）→ 写 userVerdict → 触发 Reconciler 重组 + 校准更新。
- **`TimeMachine.tsx`**：基于 `events` 事件溯源，时间轴 slider 重建任意时刻图状态，回放"理解如何演变"。
- **`ShareCard.tsx`**：`gl.domElement.toDataURL()` 截图，**脱敏**（只留形状/星团，隐藏 statement 文本），生成可下载/可分享卡。
- **`LoadingNebula.tsx`**：首次加载（含 embedding 模型下载）时播放"星云正在你的设备里凝结成形"动画——把等待转成体验（呼应原题任务3）。

---

## 8. serverless 函数

| 路由 | 方法 | 入参 | 出参 | 说明 |
|---|---|---|---|---|
| `/api/infer` | POST | `{ messages, graph }` | `CognitiveGraph` 增量 | 跑五 Agent pipeline；用服务端 key；限额 |
| `/api/confront` | POST(stream) | `{ nodeId, userArgument, graph }` | SSE：reasoning 流 + 最终 `{ revisedNodes, events }` | 对质审议（思考态）|
| `/api/embed` | POST | `{ texts }` | `{ vectors }` | **仅 cloud embedding 模式**用；local 模式不调 |

- LLM 路由从 env 读配置经 `factory` 取 provider。
- 加简单速率限制（`DEMO_DAILY_LIMIT`，按 IP/全局计数）防演示 key 被刷。
- **默认（local embedding）下，embedding/UMAP/衰减/渲染全在前端**，不进 serverless；仅切到 cloud embedding 时才用 `/api/embed`。

---

## 9. 部署（Vercel）

- `vercel` 连接仓库自动部署；serverless 函数即 `app/api/*/route.ts`。
- 环境变量在 Vercel 面板配置（见 `.env.example`）。
- 预置数据是静态的 → **无 key 也能看完整星云与预设对质**；现场实时推断走 `DEMO_DAILY_LIMIT` 限额 key。
- 产出公开 URL，分享链接即可演示。

---

## 10. 里程碑与任务（同步进 `开发进度.md`，逐项带 DoD）

> 颗粒度：每个任务力求一个上下文窗口内做完，做完即更新 `开发进度.md`。

### M0 项目脚手架
- **T0.1** 初始化 Next.js+TS+Tailwind。**DoD**：`npm run dev` 首页可访问。
- **T0.2** 安装第 2 节全部依赖。**DoD**：`npm run build` 通过。
- **T0.3** 建目录结构 + `.env.example` + `next.config`(允许 transformers.js wasm)。**DoD**：目录与配置就位。
- **T0.4** 写 `core/types.ts` + `core/schemas.ts`（第 4 节）。**DoD**：类型编译通过；有 1 个 Zod 校验单测。（注：契约文件已由 Opus 预置，本任务实为验证 `tsc --noEmit` + 补 schema 单测）
- **T0.5** 搭测试基础设施：vitest 配置 + `tests/mocks`(MockLLMProvider/MockEmbeddingProvider) + `tests/fixtures` + 跑通 schema 冒烟测试。**DoD**：`npm run test` 绿（对应《测试方案.md》第 2 节）。

### M1 认知引擎核心
- **T1.1** `LLMProvider` 接口 + `openai-compatible` adapter + `factory`；建 `/api/infer` 骨架返回 mock。**DoD**：调用 DeepSeek 返回结构化 JSON 并过 Zod。
- **T1.2** Observer。**DoD**：给定对话片段输出 facts+signals，带 messageId。
- **T1.3** Inferer。**DoD**：输出 inference 节点，单证据置信度≤0.6，evidence 指回原句。
- **T1.4** Skeptic。**DoD**：能给节点打 skepticFlag/isUncanny 并过滤。
- **T1.5** Reconciler。**DoD**：给矛盾对输出 revised + EvolutionEvent。
- **T1.6** Reflector。**DoD**：输出全局一致性/偏见/衰减候选。
- **T1.7** `pipeline.ts` 串联 + 重试 + 主动提问。**DoD**：输入对话 → 输出完整 CognitiveGraph 增量。

### M2 可信三件套 + 语义空间
- **T2.1** `calibration.ts`（先验规则 + 分桶在线校准 + 曲线导出）。**DoD**：confidence 经校准调整；曲线可取。
- **T2.2** `embedding/`：EmbeddingProvider 抽象 + local-minilm(worker+transformers.js) + factory + 缓存（cloud adapter 留接口可后补）。**DoD**：默认本地 statement→向量，二次命中缓存；业务层只依赖接口。
- **T2.3** `layout.ts`（UMAP + 星团引力）。**DoD**：节点得 3D 坐标，同系统聚拢。
- **T2.4** `decay.ts`。**DoD**：按时间算 opacity/下沉，超阈标 pruned。

### M3 数据入口
- **T3.1** `db.ts`(Dexie) 持久化层。**DoD**：节点/事件/消息本地存取。
- **T3.2** `seed.ts` 预置星云（含 1~2 恐怖谷 + 预设对质脚本）。**DoD**：打开即见丰满星云，无 key 可看。
- **T3.3** 现场补一句流。**DoD**：输入一句 → 调 infer → 星云生长。
- **T3.4** `import.ts` 导入解析。**DoD**：上传 ChatGPT/Claude 导出 → 跑 pipeline。

### M4 认知身体
- **T4.1** `Nebula.tsx`+shader+bloom。**DoD**：节点渲染成发光呼吸粒子。
- **T4.2** `visualMapping.ts`。**DoD**：亮度=置信度、抖动=质疑等映射生效。
- **T4.3** 五星团分区+颜色+hover 出脑区。**DoD**：五区可辨，hover 显真实脑区。
- **T4.4** `NodeInspector`（证据高亮 + 否决权）。**DoD**：点节点看证据，可确认/否认并触发更新。
- **T4.5** `Confrontation` 状态机（点亮→液化→审议流式→重组→沉积）。**DoD**：一句话触发完整重组 + 侧栏思考态。

### M5 演变层 + 分享 + 加载
- **T5.1** `TimeMachine`。**DoD**：滑动时间轴回放理解演变。
- **T5.2** `ShareCard`（脱敏）。**DoD**：一键生成可下载星云卡。
- **T5.3** `LoadingNebula`。**DoD**：首次加载有凝结动画掩盖模型下载。

### M6 打磨 + 开源就绪
- **T6.1** 性能优化（粒子数/shader/worker）。**DoD**：中端笔记本流畅(≥30fps)。
- **T6.2** README + 可配置文档 + 部署指南。**DoD**：他人 clone 可跑通、可换模型(改 .env)。
- **T6.3** 恐怖谷文案与否决兜底打磨。**DoD**：预置含发毛推断且否决可化解。
- **T6.4** Vercel 部署。**DoD**：公开 URL 可演示。

---

## 11. 给开发者的注意事项（常见坑）

1. **transformers.js 在 Next.js**：需在 `next.config.js` 处理 wasm/worker；模型加载放 Web Worker 避免阻塞主线程。
2. **r3f 性能**：节点用单个 `Points`+attributes，**勿**每节点一个 mesh；动画在 shader/useFrame 内，勿在 React 每帧 setState。
3. **LLM 输出不可信**：一律 Zod 校验 + 重试；prompt 里给 JSON 示例；DeepSeek 用 JSON 模式。
4. **校准别造假**：confidence 必须来自规则+真实命中率，不能让 LLM 直接吐最终分（违反项目红线）。
5. **视觉红线**：任何动效必须映射一个真实内部量，禁止纯装饰（见 `有机本体设计.md` 第六节）。
6. **可配置红线**：业务层禁止 import 任何具体厂商 SDK，只依赖 `LLMProvider` / `EmbeddingProvider` 接口。
7. **隐私红线**：用户数据只进 IndexedDB，serverless 不落库。
8. **每个任务完成更新 `开发进度.md`**（无 git，落盘是唯一安全网）。
